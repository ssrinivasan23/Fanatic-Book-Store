import { HeadingdirectiveDirective } from './headingdirective.directive';

describe('HeadingdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadingdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
